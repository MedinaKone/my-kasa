import CardContainer from "../components/CardContainer";
import "../styles/gallery.scss"

const Gallery = () => {
  return (
    <div>
      <CardContainer />
    </div>
  );
};

export default Gallery;
